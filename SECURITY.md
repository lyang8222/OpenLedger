# 安全策略

OpenLedger 以本地优先、隐私至上为核心原则。本文件说明安全承诺与漏洞报告方式。

## 安全承诺

- 无服务端组件、无遥测、无网络请求（当前版本）。
- 敏感数据在本机加密存储，密钥存放于 Keychain，仅本设备可用。
- 加密导出文件使用用户口令派生密钥，服务器或第三方无法解密。
- 多端同步仅支持 AirDrop 近距传输，全程不经过任何中转服务器。

## 支持的版本

- 开发阶段（0.x）：以 `main` 分支最新代码为准。
- 正式发布后：支持最近两个稳定版本。

## 报告漏洞

请通过以下方式之一报告，**不要**在公开 Issue 中提交漏洞细节：

- GitHub 私有漏洞报告功能（仓库公开后启用）。
- 维护者邮箱：[TODO：待仓库公开后填写]。

建议报告内容：

1. 影响范围与严重程度评估；
2. 复现步骤（尽量最小化）；
3. 相关版本、设备与系统版本。

## 加密设计文档

加密方案与密钥体系说明将随开发推进补充到 `docs/` 目录。
